import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  text: {
    fontSize: 40,
    color: "blue"
  },

  mainContainer: {
    flex: 1,
  },

  flatContainer: {
    paddingVertical: 100,
  }
});